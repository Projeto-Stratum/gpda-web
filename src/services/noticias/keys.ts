import { QueryKey } from "@tanstack/react-query";

export const keyGetNoticias = (): QueryKey => [`noticias`];
export const keyGetRockets = (): QueryKey => [`rockets`];
export const keyGetShips = (): QueryKey => [`ships`];
